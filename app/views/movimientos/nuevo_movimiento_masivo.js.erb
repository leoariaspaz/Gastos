function getRow(argument) {
	var h = $('<%= escape_javascript render "movimientos/carga_masiva/movimiento", movimiento: @movimiento %>');
	h.find('#loader').hide();
	return h;
}

//$("form #movimientos").append(getRow());
$("form #movimientos .row").last().before(getRow());
$('#nuevo-mov .loader').hide();